spring:
  application:
    name: pcs-duty
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yaml
        shared-configs:
          - dataId: mongo.yaml
            refresh: true
