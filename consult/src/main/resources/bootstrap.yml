spring:
  application:
    name: pcs-consult
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yaml
        shared-configs:
          - dataId: mongo.yaml
            refresh: true
