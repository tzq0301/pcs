spring:
  application:
    name: pcs-visit
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yaml
        shared-configs:
          - dataId: mongo.yaml
            refresh: true
